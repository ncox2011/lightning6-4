const lastName = "Cox"
//Create a function to count the letters in the string

if (lastName.length >= 7) {
        console.log("I can't pronounce this last name");
    } else {
        console.log(lastName);
    }


